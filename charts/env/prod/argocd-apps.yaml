# Argo CD Application set for the hivemind-prod cluster ("app of apps").
#
# GENERATED by scripts/generate-argocd-apps.sh -- do not hand-edit.
# Re-run the script after adding/removing a chart under charts/env/prod/,
# or after changing a chart's argocd-params.env.
#
# Apply once Argo CD itself is running (see
# charts/env/prod/central-services/argocd/README.md for the bootstrap
# `helm install`):
#
#   kubectl apply -n argocd -f charts/env/prod/argocd-apps.yaml
#
# Prerequisite: Argo CD needs read access to this repo (it's private).
# Register it once, e.g.:
#
#   argocd repo add https://github.com/chayma1205/hivemind-challenge.git \
#     --username <gh-user> --password <gh-PAT-with-repo-scope>
#
# Each Application below is independent -- a missing prerequisite in one
# chart (see that chart's README) only leaves that Application
# OutOfSync/Degraded, it doesn't block the others.
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/chayma1205/hivemind-challenge.git
    targetRevision: main
    path: charts/env/prod/central-services/argocd
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aws-load-balancer-controller
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/chayma1205/hivemind-challenge.git
    targetRevision: main
    path: charts/env/prod/critical/aws-load-balancer-controller
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: karpenter
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/chayma1205/hivemind-challenge.git
    targetRevision: main
    path: charts/env/prod/critical/karpenter
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metrics-server
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/chayma1205/hivemind-challenge.git
    targetRevision: main
    path: charts/env/prod/critical/metrics-server
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: greeter
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/chayma1205/hivemind-challenge.git
    targetRevision: main
    path: charts/env/prod/apps/greeter
    helm:
      valueFiles:
        - values.yaml
      parameters:
        - name: image.repository
          value: "324464328617.dkr.ecr.us-east-1.amazonaws.com/hivemind-greeter"
        - name: image.tag
          value: "0834255"
        - name: env.HELLO_TAG
          value: "hivemind-prod"
  destination:
    server: https://kubernetes.default.svc
    namespace: greeter
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
