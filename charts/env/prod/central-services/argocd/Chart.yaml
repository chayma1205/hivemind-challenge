apiVersion: v2
name: argocd
description: Wrapper chart that installs Argo CD as a central/shared service for the cluster
type: application
version: 0.1.0
appVersion: "3.5.3"

dependencies:
  - name: argo-cd
    version: "10.9.1"
    repository: https://argoproj.github.io/argo-helm
