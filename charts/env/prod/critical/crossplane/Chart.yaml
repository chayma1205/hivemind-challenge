apiVersion: v2
name: crossplane
description: Provisions Crossplane (Kubernetes-native control plane for AWS infra) for the prod EKS cluster
type: application
version: 0.1.0
appVersion: "2.4.1"

dependencies:
  - name: crossplane
    version: "2.4.1"
    repository: https://charts.crossplane.io/stable
